# Snapshot bundles are hash-verified byte artifacts. Preserve the byte form
# recorded by each manifest across checkout on every platform.
data/**/notices.json -text
data/**/detail.html -text
data/**/expected.json text eol=crlf
